version: "3.9"

services:
  api:
    build: .
    container_name: dovi_api_api
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - DJANGO_SETTINGS_MODULE=dovi_api.settings
  
